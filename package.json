{
  "name": "vox-server",
  "version": "1.0.0",
  "description": "Vox proximity chat server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "livekit-server-sdk": "^2.6.1",
    "socket.io": "^4.6.1"
  }
}
